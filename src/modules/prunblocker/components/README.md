# PR Unblocker — module-scoped components.
#
# Module isolation rules:
#  - import ONLY from `@/components/ui` and `@/lib`
#  - never import another module's folder
#  - every query path lives in the `module_prunblocker` schema