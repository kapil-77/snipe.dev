import type { RouteObject } from 'react-router-dom';

/**
 * Routes owned by the Envsync module.
 * EMPTY while "coming soon" — the shell renders the generic blueprint.
 */
export const envsyncRoutes: RouteObject[] = [];