import type { RouteObject } from 'react-router-dom';

/**
 * Routes owned by the Onboardtime module.
 *
 * EMPTY while the module is "coming soon" — the shell renders the generic
 * blueprint screen. When this module is built out, its components register
 * their routes here and the app router picks them up additively.
 */
export const onboardtimeRoutes: RouteObject[] = [];