# Onboardtime — module-scoped components.
#
# Rules for this folder (module isolation):
#  - import ONLY from `@/components/ui` and `@/lib`
#  - never import another module's folder (`@/modules/prunblocker/...`)
#  - every table touched lives in the `module_onboardtime` schema